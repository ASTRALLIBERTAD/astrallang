fn main() {
    let x = 5;
    let y = 10;
    print(y);
    let y = 59;
    let q = "ppk";
    while ( x < y) {
        print(q);
    }
    let r = 8;
    cow("jake", r);   
}

fn cow(name: string, num: i8) {
    let u = num + 99;
    print(u);
    let x = true;

    if (x) {
        print(name);
    }
}